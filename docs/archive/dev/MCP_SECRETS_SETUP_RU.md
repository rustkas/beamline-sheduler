# Настройка Секретов для Cursor MCP

## Обзор

Cursor IDE читает переменные окружения из системных переменных для использования в `mcp.json` через синтаксис `${VARIABLE_NAME}`. Этот документ объясняет, как безопасно настроить секреты для MCP серверов.

---

## Как Cursor IDE Работает с Переменными Окружения

### Механизм Подстановки

Cursor IDE читает переменные окружения из:
1. **Системных переменных окружения** (основной способ)
2. **Переменных окружения процесса** (наследуются от родительского процесса)
3. **Переменных из shell** (если Cursor запущен из терминала)

**Синтаксис в mcp.json:**
```json
{
  "env": {
    "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
  }
}
```

Cursor IDE автоматически подставляет значение переменной `${GITHUB_TOKEN}` из системных переменных окружения.

---

## Способы Установки Переменных Окружения

### Способ 1: Системные Переменные Окружения (Рекомендуется)

#### Linux/macOS/WSL

**Вариант A: Для текущей сессии (временный)**
```bash
export GITHUB_TOKEN="ghp_your_token_here"
```

**Вариант B: Постоянно (рекомендуется)**

Добавить в `~/.bashrc` или `~/.zshrc`:
```bash
# GitHub Token for Cursor MCP
export GITHUB_TOKEN="ghp_your_token_here"
```

Затем перезагрузить shell:
```bash
source ~/.bashrc
# или
source ~/.zshrc
```

**Вариант C: Через ~/.profile (для всех shell)**
```bash
# Добавить в ~/.profile
export GITHUB_TOKEN="ghp_your_token_here"
```

---

#### Windows

**Через PowerShell (для текущей сессии):**
```powershell
$env:GITHUB_TOKEN = "ghp_your_token_here"
```

**Через System Properties (постоянно):**
1. Открыть "System Properties" → "Environment Variables"
2. Добавить новую переменную:
   - Name: `GITHUB_TOKEN`
   - Value: `ghp_your_token_here`
3. Перезапустить Cursor IDE

---

### Способ 2: .env Файл (Альтернатива)

**Примечание**: Cursor IDE не читает `.env` файлы автоматически для глобального `mcp.json`, но можно использовать для проектного `mcp.json`.

**Создать `.env` в проекте:**
```bash
# .env (не коммитить в Git!)
GITHUB_TOKEN=ghp_your_token_here
```

**Загрузить в shell перед запуском Cursor:**
```bash
export $(cat .env | xargs)
cursor .
```

**Или использовать `dotenv` в shell:**
```bash
# Установить dotenv-cli
npm install -g dotenv-cli

# Запустить Cursor с загруженными переменными
dotenv cursor .
```

---

### Способ 3: Cursor Settings (Если Поддерживается)

**Проверить, поддерживает ли Cursor IDE настройку переменных окружения через Settings:**

1. Открыть Cursor Settings (`Ctrl+,` или `Cmd+,`)
2. Найти раздел "Environment Variables" или "Secrets"
3. Добавить `GITHUB_TOKEN` со значением

**Примечание**: Эта функция может быть недоступна в текущей версии Cursor IDE.

---

## Рекомендуемый Подход для Проекта

### Для Linux/WSL (Ваш Случай)

**Шаг 1: Создать GitHub Personal Access Token**

1. Перейти на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создать новый token с правами:
   - `repo` (для работы с репозиториями)
   - `workflow` (для работы с workflows)
   - `read:org` (если нужен доступ к организациям)

**Шаг 2: Добавить в ~/.bashrc или ~/.zshrc**

```bash
# Добавить в конец файла ~/.bashrc
# GitHub Token for Cursor MCP
export GITHUB_TOKEN="ghp_your_actual_token_here"
```

**Шаг 3: Перезагрузить Shell**

```bash
source ~/.bashrc
# или перезапустить терминал
```

**Шаг 4: Проверить**

```bash
echo $GITHUB_TOKEN
# Должен показать токен (или пустую строку, если не установлен)
```

**Шаг 5: Перезапустить Cursor IDE**

После установки переменной окружения перезапустить Cursor IDE, чтобы она подхватила новую переменную.

---

## Безопасность

### ✅ **Правильно** (Безопасно)

1. **Хранить в системных переменных окружения**:
   ```bash
   export GITHUB_TOKEN="ghp_..."
   ```

2. **Использовать в mcp.json через подстановку**:
   ```json
   {
     "env": {
       "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
     }
   }
   ```

3. **Добавить в .gitignore** (если используете .env):
   ```
   .env
   .env.local
   ```

### ❌ **Неправильно** (Небезопасно)

1. **Хранить токен напрямую в mcp.json**:
   ```json
   {
     "env": {
       "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_actual_token_here"  // ❌ НЕ ДЕЛАТЬ!
     }
   }
   ```

2. **Коммитить токены в Git**:
   ```bash
   git add .env  # ❌ Если .env содержит реальные токены
   git commit -m "Add env file"
   ```

3. **Хранить в публичных файлах**:
   - ❌ В README.md
   - ❌ В документации
   - ❌ В комментариях кода

---

## Проверка Настройки

### Тест 1: Проверка Переменной Окружения

```bash
# В терминале
echo $GITHUB_TOKEN
# Должен показать токен (или пустую строку)
```

### Тест 2: Проверка в Cursor IDE

1. Открыть Cursor IDE
2. Открыть MCP серверы (если есть UI для этого)
3. Проверить, что GitHub MCP работает без ошибок

### Тест 3: Проверка через MCP Tool

Попробовать вызвать GitHub MCP tool через AI:
```
AI: "Создай issue в GitHub репозитории"
→ Если работает без ошибок авторизации - токен настроен правильно
```

---

## Troubleshooting

### Проблема: `${GITHUB_TOKEN}` не подставляется

**Решение 1**: Проверить, что переменная установлена:
```bash
echo $GITHUB_TOKEN
```

**Решение 2**: Перезапустить Cursor IDE после установки переменной

**Решение 3**: Проверить, что используется правильный синтаксис в mcp.json:
```json
{
  "env": {
    "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
  }
}
```

---

### Проблема: Переменная не сохраняется после перезапуска терминала

**Решение**: Добавить в `~/.bashrc` или `~/.zshrc`:
```bash
export GITHUB_TOKEN="ghp_your_token_here"
```

Затем перезагрузить shell:
```bash
source ~/.bashrc
```

---

### Проблема: Cursor IDE не видит переменную

**Решение 1**: Убедиться, что Cursor запущен из терминала с установленной переменной:
```bash
export GITHUB_TOKEN="ghp_your_token_here"
cursor .
```

**Решение 2**: Перезапустить Cursor IDE полностью (не только перезагрузить окно)

**Решение 3**: Проверить, что переменная установлена глобально (в ~/.bashrc или ~/.profile)

---

## Примеры Конфигурации

### Глобальный mcp.json (~/.cursor/mcp.json)

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
```

**Переменная `GITHUB_TOKEN` должна быть установлена в системных переменных окружения.**

---

### Проектный mcp.json (.cursor/mcp.json)

```json
{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/servers/main/schema/mcp_config_schema.json",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
```

**Переменная `GITHUB_TOKEN` должна быть доступна в окружении, где запускается Cursor IDE.**

---

## Дополнительные Секреты

### Для Других MCP Серверов

**SQL MCP:**
```json
{
  "sql": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-sql"],
    "env": {
      "DATABASE_URL": "${DATABASE_URL}"
    }
  }
}
```

**Установить переменную:**
```bash
export DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
```

---

**PostHog MCP:**
```json
{
  "posthog": {
    "command": "npx",
    "args": ["-y", "mcp-remote@latest", "https://mcp.posthog.com/sse"],
    "env": {
      "POSTHOG_AUTH_HEADER": "${POSTHOG_AUTH_HEADER}"
    }
  }
}
```

**Установить переменную:**
```bash
export POSTHOG_AUTH_HEADER="Bearer phx_your_token_here"
```

---

## Резюме

### ✅ **Рекомендуемый Подход**

1. **Создать GitHub Personal Access Token**
2. **Добавить в ~/.bashrc или ~/.zshrc**:
   ```bash
   export GITHUB_TOKEN="ghp_your_token_here"
   ```
3. **Перезагрузить shell**: `source ~/.bashrc`
4. **Перезапустить Cursor IDE**
5. **Использовать в mcp.json**: `"${GITHUB_TOKEN}"`

### 🔒 **Безопасность**

- ✅ Хранить токены в системных переменных окружения
- ✅ Использовать подстановку `${VARIABLE}` в mcp.json
- ✅ Не коммитить токены в Git
- ✅ Не хранить токены в публичных файлах

### 📝 **Проверка**

```bash
# Проверить переменную
echo $GITHUB_TOKEN

# Перезапустить Cursor IDE
# Попробовать использовать GitHub MCP через AI
```

---

## Ссылки

- [GitHub Personal Access Tokens](https://github.com/settings/tokens)
- [Cursor IDE Documentation](https://cursor.sh/docs)
- [MCP Protocol Specification](https://modelcontextprotocol.io)

