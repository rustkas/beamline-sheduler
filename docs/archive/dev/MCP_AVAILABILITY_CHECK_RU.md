# Проверка Доступности MCP Серверов

## Результаты Проверки (2025-11-16)

### ✅ **ДОСТУПНЫЕ** MCP Серверы

#### 1. **GitHub MCP** ✅

**Пакет**: `@modelcontextprotocol/server-github`  
**Версия**: 2025.4.8  
**Статус**: ✅ **ДОСТУПЕН**  
**Установка**: `npx -y @modelcontextprotocol/server-github`

**Конфигурация**:
```json
{
  "github": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
    }
  }
}
```

**Статус**: ✅ **УЖЕ НАСТРОЕН И РАБОТАЕТ**

---

#### 2. **Memory MCP** ✅

**Пакет**: `@modelcontextprotocol/server-memory`  
**Версия**: 2025.9.25  
**Статус**: ✅ **ДОСТУПЕН**  
**Описание**: MCP server for enabling memory for Claude through a knowledge graph  
**Установка**: `npx -y @modelcontextprotocol/server-memory`

**Конфигурация**:
```json
{
  "memory": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"]
  }
}
```

**Рекомендация**: ✅ **МОЖНО ДОБАВИТЬ** - улучшит контекст между сессиями

---

#### 3. **File System MCP** ✅

**Пакет**: `@modelcontextprotocol/server-filesystem`  
**Статус**: ✅ **ДОСТУПЕН**  
**Описание**: Расширенные операции с файлами

**Конфигурация**:
```json
{
  "filesystem": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem"]
  }
}
```

**Рекомендация**: 💡 **МОЖНО ДОБАВИТЬ** - расширит возможности работы с файлами

---

#### 4. **Sequential Thinking MCP** ✅

**Пакет**: `@modelcontextprotocol/server-sequential-thinking`  
**Статус**: ✅ **ДОСТУПЕН**

**Рекомендация**: 💡 **ОПЦИОНАЛЬНО** - для сложных логических задач

---

#### 5. **Everything MCP** ✅

**Пакет**: `@modelcontextprotocol/server-everything`  
**Статус**: ✅ **ДОСТУПЕН**

**Рекомендация**: 💡 **ОПЦИОНАЛЬНО** - универсальный сервер

---

### ❌ **НЕДОСТУПНЫЕ** MCP Серверы

#### 1. **SQL MCP** ❌

**Пакет**: `@modelcontextprotocol/server-sql`  
**Статус**: ❌ **НЕ СУЩЕСТВУЕТ**  
**Ошибка**: `npm error 404 Not Found`

**Альтернативы**:
1. **Создать кастомный SQL MCP сервер** (рекомендуется)
   - Использовать PostgreSQL драйвер
   - Создать `tools/sql-mcp/` с инструментами для SQL операций
   - Пример: `tools/database/` (можно создать)

2. **Использовать готовые решения**:
   - `cursor-db-mcp-server` (если доступен)
   - Кастомные MCP серверы для PostgreSQL

**Рекомендация**: ❌ **НЕЛЬЗЯ ДОБАВИТЬ** - пакет не существует, нужен кастомный сервер

---

#### 2. **Git MCP** ❌

**Пакет**: `@modelcontextprotocol/server-git`  
**Статус**: ❌ **НЕ СУЩЕСТВУЕТ**  
**Ошибка**: `npm error 404 Not Found`

**Альтернатива**: GitHub MCP уже покрывает Git операции через GitHub API

**Рекомендация**: ❌ **НЕ НУЖЕН** - GitHub MCP уже предоставляет Git операции

---

## Итоговые Рекомендации

### ✅ **Добавить Сейчас**

1. **Memory MCP** ✅
   - Пакет существует и доступен
   - Улучшит контекст между сессиями
   - Простая установка

**Конфигурация**:
```json
{
  "memory": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"]
  }
}
```

---

### 💡 **Добавить По Необходимости**

2. **File System MCP** 💡
   - Пакет существует и доступен
   - Расширит возможности работы с файлами
   - Полезен для массовых операций

**Конфигурация**:
```json
{
  "filesystem": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem"]
  }
}
```

---

### ❌ **Не Добавлять**

1. **SQL MCP** ❌
   - Пакет не существует
   - Нужен кастомный сервер
   - Можно создать позже при необходимости

2. **Git MCP** ❌
   - Пакет не существует
   - GitHub MCP уже покрывает Git операции

---

## Обновленная Конфигурация

### Рекомендуемая Конфигурация (~/.cursor/mcp.json)

```json
{
  "mcpServers": {
    "Browser Use": {
      "url": "https://docs.browser-use.com/mcp",
      "headers": {}
    },
    "Prisma": {
      "url": "https://mcp.prisma.io/mcp",
      "headers": {}
    },
    "PostHog": {
      "command": "npx -y mcp-remote@latest https://mcp.posthog.com/sse --header Authorization:${POSTHOG_AUTH_HEADER}",
      "env": {
        "POSTHOG_AUTH_HEADER": "Bearer phx_XHphvTnORQ75MgjvJBLwPnQRCk2z2PL6LmfECtrBJszesY1"
      },
      "args": []
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
```

---

## Резюме

### ✅ **Доступные и Рекомендуемые**

1. ✅ **GitHub MCP** - уже настроен и работает
2. ✅ **Memory MCP** - можно добавить сейчас
3. 💡 **File System MCP** - можно добавить по необходимости

### ❌ **Недоступные**

1. ❌ **SQL MCP** - пакет не существует, нужен кастомный сервер
2. ❌ **Git MCP** - пакет не существует, GitHub MCP уже покрывает

---

## Следующие Шаги

1. **Добавить Memory MCP** (рекомендуется):
   - Добавить конфигурацию в `~/.cursor/mcp.json`
   - Перезапустить Cursor IDE
   - Проверить работу

2. **Рассмотреть File System MCP** (опционально):
   - Добавить при необходимости расширенных операций с файлами

3. **Создать кастомный SQL MCP** (если нужно):
   - Создать `tools/sql-mcp/` или `tools/database/`
   - Использовать PostgreSQL драйвер
   - Реализовать инструменты для SQL операций

---

## Обновление Документации

**Файл**: `docs/archive/dev/MCP_CURSOR_RECOMMENDATIONS.md`

**Изменения**:
- ❌ Удалить SQL MCP из рекомендаций (пакет не существует)
- ✅ Обновить Memory MCP с правильной информацией
- ✅ Добавить File System MCP как альтернативу
- ❌ Подтвердить, что Git MCP не нужен (GitHub MCP покрывает)

---

## Выводы

**Реально доступные MCP серверы**:
- ✅ GitHub MCP (уже настроен)
- ✅ Memory MCP (можно добавить)
- ✅ File System MCP (можно добавить)

**Недоступные MCP серверы**:
- ❌ SQL MCP (нужен кастомный сервер)
- ❌ Git MCP (не нужен, GitHub MCP покрывает)

**Рекомендация**: Добавить Memory MCP для улучшения контекста между сессиями.

