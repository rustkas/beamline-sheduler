# Рекомендации по Cursor MCP для Ускорения Разработки

## Обзор

Проект уже имеет **15 кастомных MCP серверов** с **67 инструментами**. Этот документ анализирует, какие **внешние Cursor MCP серверы** могут дополнительно ускорить разработку, автоматизировав ручные операции.

---

## Анализ Ручных Операций

### Текущие Ручные Задачи

1. **Git Операции**:
   - Создание веток, коммитов, тегов
   - Управление PR (создание, обновление, merge)
   - Git workflow операции

2. **GitHub Операции**:
   - Создание/обновление issues
   - Code review
   - Release management
   - Workflow management

3. **Работа с БД**:
   - Выполнение SQL запросов для проверки
   - Валидация схемы БД
   - Миграции

4. **Работа с Файлами**:
   - Поиск файлов по паттернам
   - Массовые операции с файлами
   - Рефакторинг кода

5. **Контекст между Сессиями**:
   - Сохранение контекста работы
   - Восстановление состояния

---

## Рекомендуемые Cursor MCP Серверы

### 🔥 **КРИТИЧЕСКИ ВАЖНЫЕ** (Высокий приоритет)

#### 1. **GitHub MCP** ⭐⭐⭐

**Что дает:**
- Управление issues (создание, обновление, закрытие)
- Управление PR (создание, обновление, merge, review)
- Code review через AI
- Release management (создание releases, тегов)
- Workflow management

**Ускорит разработку:**
- ✅ Автоматизация создания PR с описанием
- ✅ AI может анализировать code review комментарии
- ✅ Автоматизация release процесса
- ✅ Управление issues через AI

**Примеры использования:**
```
AI: "Создай PR для изменений в router с описанием"
→ AI вызывает github_create_pull_request
→ AI анализирует изменения
→ AI генерирует описание PR
→ AI создает PR автоматически

AI: "Проверь все открытые issues и обнови статусы"
→ AI вызывает github_list_issues
→ AI анализирует issues
→ AI обновляет статусы через github_update_issue
```

**Установка:**
```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
```

**Приоритет**: 🔥 **КРИТИЧЕСКИЙ** - автоматизирует много ручных операций

---

#### 2. **Git MCP** ⭐⭐⭐

**Что дает:**
- Git операции (commit, push, branch, merge, tag)
- Управление ветками
- История коммитов
- Diff операции

**Ускорит разработку:**
- ✅ AI может создавать коммиты с правильными сообщениями
- ✅ Автоматизация Git workflow
- ✅ Управление ветками через AI
- ✅ Анализ истории коммитов

**Примеры использования:**
```
AI: "Создай коммит для изменений в router с сообщением"
→ AI вызывает git_status
→ AI анализирует изменения
→ AI создает коммит через git_commit
→ AI формирует правильное сообщение коммита

AI: "Создай release ветку и тег для v1.0.0"
→ AI вызывает git_create_branch
→ AI вызывает git_create_tag
→ Все автоматически
```

**Установка:**
```json
{
  "mcpServers": {
    "git": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-git"],
      "cwd": "${workspaceFolder}"
    }
  }
}
```

**Приоритет**: 🔥 **КРИТИЧЕСКИЙ** - автоматизирует Git операции

---

### ⚡ **ВАЖНЫЕ** (Средний приоритет)

#### 3. **SQL MCP** ⭐⭐

**Что дает:**
- Выполнение SQL запросов
- Валидация схемы БД
- Миграции
- Интроспекция БД

**Ускорит разработку:**
- ✅ AI может выполнять SQL запросы для проверки данных
- ✅ Валидация схемы БД через AI
- ✅ Генерация SQL миграций
- ✅ Проверка консистентности данных

**Примеры использования:**
```
AI: "Проверь, что все tenant_id в usage_events существуют в projects"
→ AI вызывает sql_execute_query
→ AI анализирует результаты
→ AI сообщает о проблемах

AI: "Создай миграцию для добавления поля version в policies"
→ AI анализирует текущую схему
→ AI генерирует SQL миграцию
→ AI выполняет миграцию через sql_execute_migration
```

**Установка:**
```json
{
  "mcpServers": {
    "sql": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sql"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
```

**Приоритет**: ⚡ **ВАЖНЫЙ** - ускорит работу с БД

**Примечание**: Проект использует PostgreSQL, нужен MCP с поддержкой PostgreSQL

---

#### 4. **Memory MCP** ⭐⭐

**Что дает:**
- Сохранение контекста между сессиями
- Восстановление состояния работы
- Поиск по сохраненному контексту

**Ускорит разработку:**
- ✅ AI помнит контекст предыдущих сессий
- ✅ Не нужно повторно объяснять контекст
- ✅ Быстрое восстановление работы
- ✅ Поиск по истории работы

**Примеры использования:**
```
AI: "Помни, что мы работаем над CP1 Router implementation"
→ AI сохраняет контекст через memory_save
→ В следующей сессии AI восстанавливает контекст

AI: "Что мы обсуждали про HMAC chain?"
→ AI ищет в памяти через memory_search
→ AI находит релевантный контекст
```

**Установка:**
```json
{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
```

**Приоритет**: ⚡ **ВАЖНЫЙ** - улучшит контекст между сессиями

---

### 💡 **ПОЛЕЗНЫЕ** (Низкий приоритет, но могут быть полезны)

#### 5. **File System MCP** ⭐

**Что дает:**
- Расширенные операции с файлами
- Поиск файлов по паттернам
- Массовые операции

**Ускорит разработку:**
- ✅ AI может эффективно работать с файлами
- ✅ Поиск файлов по сложным паттернам
- ✅ Массовые операции рефакторинга

**Примечание**: Cursor уже имеет базовую работу с файлами, но расширенный File System MCP может дать больше возможностей

**Приоритет**: 💡 **ПОЛЕЗНЫЙ** - расширит возможности работы с файлами

---

#### 6. **Browser MCP** ⭐

**Что дает:**
- Автоматизация браузера для тестирования
- Скриншоты и snapshot
- Взаимодействие с UI

**Ускорит разработку:**
- ✅ Автоматическое тестирование UI (когда UI будет реализован)
- ✅ Скриншоты для документации
- ✅ Проверка UI функциональности

**Примеры использования:**
```
AI: "Проверь, что health endpoint возвращает правильный JSON"
→ AI открывает браузер через browser_navigate
→ AI проверяет ответ через browser_snapshot
→ AI валидирует JSON формат
```

**Приоритет**: 💡 **ПОЛЕЗНЫЙ** - будет полезен когда UI будет реализован

**Примечание**: Уже есть в проекте (`mcp_Browser_Use_SearchBrowserUse`), но можно добавить расширенный Browser MCP для автоматизации

---

## Не Рекомендуемые MCP

### ❌ **Docker MCP**

**Почему не нужен:**
- ✅ Уже есть `infrastructure` MCP сервер с Docker операциями
- ✅ `local_up`, `local_down`, `local_build` покрывают потребности
- ✅ Дублирование функциональности

---

### ❌ **Linear/Jira MCP**

**Почему не нужен:**
- ❌ Проект не использует Linear/Jira
- ❌ Используется GitHub Issues
- ❌ GitHub MCP покрывает управление задачами

---

### ❌ **Figma MCP**

**Почему не нужен:**
- ❌ Нет UI дизайна в Figma
- ❌ UI компонент не реализован
- ❌ Низкий приоритет

---

## Приоритизация

### Фаза 1: Критически Важные (Добавить Сейчас)

1. **GitHub MCP** - автоматизация PR, issues, releases
2. **Git MCP** - автоматизация Git операций

**Время установки**: 10 минут  
**Эффект**: Высокий - автоматизирует много ручных операций

---

### Фаза 2: Важные (Добавить После Фазы 1)

3. **SQL MCP** - ускорение работы с БД
4. **Memory MCP** - улучшение контекста между сессиями

**Время установки**: 10 минут  
**Эффект**: Средний - улучшит производительность

---

### Фаза 3: Полезные (Добавить По Необходимости)

5. **File System MCP** - расширенные операции с файлами
6. **Browser MCP** - автоматизация UI тестирования (когда UI будет готов)

**Время установки**: 10 минут  
**Эффект**: Низкий-Средний - дополнительные возможности

---

## Установка

### Шаг 1: Обновить `.cursor/mcp.json`

Добавить новые MCP серверы:

```json
{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/servers/main/schema/mcp_config_schema.json",
  "mcpServers": {
    // ... существующие серверы ...
    
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "git": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-git"],
      "cwd": "${workspaceFolder}"
    },
    "sql": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sql"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
```

### Шаг 2: Настроить Environment Variables

Добавить в `.env` или Cursor settings:
- `GITHUB_TOKEN` - GitHub Personal Access Token
- `DATABASE_URL` - PostgreSQL connection string

### Шаг 3: Перезапустить Cursor IDE

После добавления MCP серверов перезапустить Cursor IDE для загрузки новых серверов.

---

## Примеры Использования

### Пример 1: Автоматизация PR Workflow

**Без MCP:**
```bash
# Разработчик должен:
1. git add .
2. git commit -m "feat: ..."
3. git push origin feature-branch
4. Открыть GitHub в браузере
5. Создать PR вручную
6. Написать описание PR
7. Добавить reviewers
```

**С GitHub + Git MCP:**
```
AI: "Создай PR для изменений в router"
→ AI вызывает git_status (проверяет изменения)
→ AI вызывает git_commit (создает коммит)
→ AI вызывает git_push (пушит ветку)
→ AI вызывает github_create_pull_request (создает PR)
→ AI анализирует изменения и генерирует описание
→ AI добавляет reviewers автоматически
```

---

### Пример 2: Автоматизация Release Process

**Без MCP:**
```bash
# Разработчик должен:
1. Обновить версии вручную
2. Обновить CHANGELOG.md
3. git tag v1.0.0
4. git push --tags
5. Создать GitHub release вручную
6. Добавить release notes
```

**С GitHub + Git MCP:**
```
AI: "Подготовь релиз v1.0.0"
→ AI вызывает check_version_gates (из release-management MCP)
→ AI обновляет версии через файловые операции
→ AI обновляет CHANGELOG.md
→ AI вызывает git_create_tag
→ AI вызывает git_push (теги)
→ AI вызывает github_create_release
→ AI генерирует release notes из CHANGELOG.md
```

---

### Пример 3: Работа с БД

**Без MCP:**
```bash
# Разработчик должен:
1. Подключиться к БД вручную
2. Выполнить SQL запрос
3. Проанализировать результаты
4. Создать миграцию вручную
```

**С SQL MCP:**
```
AI: "Проверь, что все tenant_id в usage_events существуют в projects"
→ AI вызывает sql_execute_query
→ AI анализирует результаты
→ AI сообщает о проблемах
→ AI может создать миграцию для исправления
```

---

## Итоговые Рекомендации

### ✅ **Добавить Сейчас** (Фаза 1)

1. **GitHub MCP** - автоматизация PR, issues, releases
2. **Git MCP** - автоматизация Git операций

**Эффект**: Высокий - автоматизирует много ручных операций  
**Время**: 10 минут установки

---

### ✅ **Добавить После** (Фаза 2)

3. **SQL MCP** - ускорение работы с БД
4. **Memory MCP** - улучшение контекста между сессиями

**Эффект**: Средний - улучшит производительность  
**Время**: 10 минут установки

---

### ⚠️ **Не Добавлять**

- Docker MCP (уже есть infrastructure MCP)
- Linear/Jira MCP (используется GitHub Issues)
- Figma MCP (нет UI дизайна)
- PostHog MCP (не используется, см. MCP_POSTHOG_ANALYSIS.md)
- Prisma MCP (не используется, см. MCP_PRISMA_ANALYSIS.md)

---

## Метрики Улучшений

### До Добавления MCP:
- ❌ Ручное создание PR (5-10 минут)
- ❌ Ручное управление Git операциями
- ❌ Ручная работа с БД
- ❌ Потеря контекста между сессиями

### После Добавления MCP:
- ✅ Автоматическое создание PR (30 секунд)
- ✅ Автоматизация Git операций
- ✅ AI-ассистированная работа с БД
- ✅ Сохранение контекста между сессиями

**Ожидаемое ускорение**: 30-50% времени на рутинные операции

---

## Заключение

**Рекомендуется добавить**:
1. **GitHub MCP** (критически важно)
2. **Git MCP** (критически важно)
3. **SQL MCP** (важно)
4. **Memory MCP** (важно)

Эти MCP серверы дополнят существующие 15 кастомных MCP и значительно ускорят разработку, автоматизировав ручные операции.

